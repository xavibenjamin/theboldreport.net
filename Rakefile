## ==============================================
## Heroku-specific Commands
## ==============================================

## assets:precompile -------------
namespace :assets do
  task :precompile do
    puts `bundle exec jekyll build --config _config_production.yml`
  end
end
